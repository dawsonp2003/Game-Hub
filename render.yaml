# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: game-arcade-signaling
    runtime: node
    rootDir: server
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    plan: free
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production

  - type: web
    name: game-arcade-web
    runtime: static
    rootDir: web
    buildCommand: npm install --include=dev && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_SIGNALING_URL
        value: game-arcade-signaling.onrender.com
      # Set these in the Render dashboard (Static Site → Environment).
      # Vite bakes them in at build time, so redeploy after changing them.
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
